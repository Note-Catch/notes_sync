# notes-sync

## Dev environment setup guide

```shell
poetry install
poetry run pre-commit install
```

### Useful commands

* Run linters
```shell
make lint
```

* Format the code
```shell
make format
```
